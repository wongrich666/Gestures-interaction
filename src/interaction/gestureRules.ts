import { PINCH_DEBOUNCE_MS, PINCH_THRESHOLD, TRAIL_MIN_DISTANCE } from '../core/config'

export const gestureRules = {
  pinchThreshold: PINCH_THRESHOLD,
  pinchDebounceMs: PINCH_DEBOUNCE_MS,
  trailMinDistance: TRAIL_MIN_DISTANCE,
}
